import React from 'react'
import Loader from './Loader'
import StyledConnecting from './styled/StyledConnecting'

export default function Connecting() {
  return (
    <StyledConnecting>
        <Loader/>
    </StyledConnecting>
  )
}
